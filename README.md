# Andor One Page Website

## Environment

- [ ] Install plugins: prettier, css nesting
- [ ] VS Code configuration: wrap, format on save, editor default formatter prettier, prettier one attribute per line, prettier bracket same line

## TODO

- [ ] HTML: tags, attributes and values, nesting
- [ ] HTML: semantic HTML (no divs) and add assets (images, videos)
- [ ] CSS: browser/default CSS, selectors, properties (logical) and values
- [ ] CSS: global variables (colors): base and incident variables
- [ ] CSS: box model: margin, padding, border + box-sizing
- [ ] CSS: kill block start margins
- [ ] CSS: `<html>` and font stacks (global variables)
- [ ] CSS: `<body>` background images, nesting and media queries
- [ ] CSS: `<a>` default styles
- [ ] CSS: responsive web design: media queries, fluid layout, fluid media
- [ ] CSS: layout tools: flexbox, grid, positioning
- [ ] CSS: fluid media (images and videos)
- [ ] CSS: @font-face and custom fonts
- [ ] CSS: sections (clamp) and local variables
- [ ] CSS: containers, variants and local variables
- [ ] CSS: components
  - [ ] header (flexbox and auto margins)
  - [ ] logo
  - [ ] nav and transitions
  - [ ] intro
  - [ ] titles
  - [ ] button
  - [ ] trailer and caption
  - [ ] quote
  - [ ] characters list
  - [ ] characters and transitions
  - [ ] footer
